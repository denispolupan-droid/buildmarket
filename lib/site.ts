// Єдине джерело правди для загальносайтових констант.

// Базовий URL сайту. Раніше хардкодився як 'https://fixline.com.ua' у десятках
// файлів — для нового коду використовуй цю константу (зміна домену/стейджинг в одному місці).
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://fixline.com.ua';

// Мінімальна сума оптового замовлення, грн. Має збігатися між серверною перевіркою
// на checkout (app/api/orders) і UI кошика/каталогу — інакше клієнт проходить кошик
// і отримує 400 від API.
export const WHOLESALE_MIN = 3000;

// Кількість відділень Нової Пошти. Цифра ЗОВНІШНЯ — не з нашої бази, тому живе
// тут із датою звірки, а не хардкодом у розмітці: інакше за рік ніхто не згадає,
// звідки вона взялася і чи ще актуальна.
// Звірено: 2026-08-09, за даними Нової Пошти.
export const NP_BRANCHES = 28000;

// Точки видачі ROZETKA — другий перевізник сайту. На відміну від НП, цифру можна
// не оцінювати, а порахувати: GET /api/department?carrier=<Rozetka> віддає
// total_count. Тримаємо тут із датою, щоб у розмітці не з'явився черговий
// «понад 600», який ніхто не перевірить.
// Звірено: 2026-08-12, GET https://rz-delivery.rozetka.ua/api/department.
export const RZ_POINTS = 619;
export const RZ_CITIES = 261;
