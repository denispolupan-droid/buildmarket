// ЛЕГАСІ-URL Prom-фіда. Канонічний фід — /api/prom-feed (наценки, комісії,
// on_prom-фільтр, атрибути). Раніше тут жила окрема спрощена копія генератора
// БЕЗ логіки націнок — якщо Prom був налаштований на цей URL, ціни віддавались
// без торгової надбавки. Тепер обидва URL віддають один канонічний фід.
export { GET } from '../../prom-feed/route';
